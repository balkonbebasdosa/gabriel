# backend (owner: adit)

Spring Boot 3.x API: transcript ingestion, Postgres persistence, orchestration call to
`ai-service`, auth (stretch goal).

Consumes `ai-service` via `POST /analyze` — see [`../docs/api-contract.md`](../docs/api-contract.md).
